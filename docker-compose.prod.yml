services:
  web:
    image: adwais-web:local
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        VITE_OIDC_AUTHORITY: ${OIDC_AUTHORITY:-}
        VITE_OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
        VITE_OIDC_SCOPE: ${OIDC_SCOPE:-openid profile email}
        VITE_DEMO_MODE: ${DEMO_MODE:-false}
        VITE_SSO_BUTTON_LABEL: ${SSO_BUTTON_LABEL:-Sign in with SSO}
        VITE_SSO_BUTTON_LOGO_URL: ${SSO_BUTTON_LOGO_URL:-}
    expose:
      - "80"
    networks:
      - edge
      - app
    depends_on:
      - api
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  api:
    image: adwais-api:local
    build:
      context: .
      dockerfile: apps/server/ADWAIS/src/Api/Dockerfile
    expose:
      - "8080"
    environment:
      ConnectionStrings__AnalyticsDb: Host=db;Database=analyticsdb;Username=postgres;Password=${DB_PASSWORD}
      Webhooks__MotasticApiKey: ${MOTASTIC_API_KEY}
      Webhooks__NewsletterApiKey: ${NEWSLETTER_API_KEY}
      Authentication__OidcAuthority: ${OIDC_AUTHORITY}
      Authentication__OidcAudience: ${OIDC_AUDIENCE}
      Authentication__EnableDemoAccess: ${DEMO_MODE:-false}
      Authentication__KioskJwtSecret: ${KIOSK_JWT_SECRET}
      ASPNETCORE_ENVIRONMENT: Production
      FeatureToggles__EnableRuntimeDataSeeding: ${ENABLE_RUNTIME_DATA_SEEDING:-true}
    volumes:
      - dp-keys-prod:/app/dp-keys
    networks:
      - app
      - data
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: analyticsdb
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata-prod:/var/lib/postgresql/data
    networks:
      admin:
        gw_priority: 1
      data:
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d analyticsdb"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:${CLOUDFLARED_IMAGE_TAG:-latest}
    command:
      - tunnel
      - --no-autoupdate
      - run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    networks:
      - edge
    depends_on:
      web:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata-prod:
  dp-keys-prod:

networks:
  admin:
  edge:
  app:
  data:
    internal: true
