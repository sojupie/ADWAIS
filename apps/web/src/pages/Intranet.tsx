import { FactPanel } from '../components/common/FactPanel';

export function Intranet() {
    return (
        <section className="dashboard-placeholder" aria-label="Intranet">
            <FactPanel label="Intranet" value="In progrses!" />
        </section>
    );
}