# Dependencies
node_modules/

# .NET build
**/bin/
**/obj/

# Environment and Logs
# src/.env is tracked — contains only public Azure AD identifiers (tenant ID, client ID)
# Use .env.local for machine-specific overrides
*.log

# OS files
.DS_Store
Thumbs.db

# Build artifacts (Note the leading slashes to prevent deep-ignoring)
/target/
/dist/
*.jar

# IDEs
.idea/
.vscode/

# Unit test reports
TEST*.xml

# Executables
*.app
*.exe
*.war

# Large media files
*.mp4
*.tiff
*.avi
*.flv
*.mov
*.wmv

/*.sln.DotSettings.user
appsettings.json
# appsettings.Development.json is tracked — contains only non-secret dev defaults
# appsettings.Production.json and appsettings.Staging.json must never be committed
appsettings.Production.json
appsettings.Staging.json
